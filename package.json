{
  "name": "brainstormor",
  "version": "1.1.0",
  "private": true,
  "description": "An offline, portable brainstorming canvas",
  "scripts": {
    "build": "node scripts/build.mjs",
    "test": "node --test tests/*.test.cjs",
    "start": "python3 -m http.server 4173 --bind 127.0.0.1"
  },
  "devDependencies": {
    "mermaid": "12.0.0"
  }
}
